# Whishlist file; what we would do if we had someone funded to work full time.
using Statistics

function select_features(Y; temporal = [lag1ac, max_frequency], statistical = [std, mean, kyrtosis], dynamical = [perment, complexity])
end

function analyze_feature_convergence()
end